@model List<doanweb.Models.tbl_giohang>

<li class="nav-item dropdown no-arrow mx-1">
    <a class="nav-link dropdown-toggle" href="#" id="alertsDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="fas fa-bell fa-fw"></i>
        <!-- Counter - Alerts -->
        <span class="badge badge-danger badge-counter">
            @Model.Count+
        </span>
    </a>
    <!-- Dropdown - Alerts -->
    <div class="dropdown-list dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="alertsDropdown">
        <h6 class="dropdown-header">
            Thông Báo Đơn Hàng Mới
        </h6>
        @{
    foreach (var item in Model)
    {
        <a class="dropdown-item d-flex align-items-center" href="@Url.Action("QuanLyGioHang_Sua", "Admin_QLGH", new { @MaSua = item.Id })">
            <div class="mr-3">
                <div class="icon-circle bg-warning">
                    <i class="fas fa-luggage-cart text-black"></i>
                </div>
            </div>
            <div>
                <div class="small text-gray-500">@item.TenKhachHang</div>
                <span class="font-weight-bold">@item.TenSanPham</span>
            </div>
        </a>
    }
}
        <a class="dropdown-item text-center small text-gray-500" href="@Url.Action("QuanLyGioHang","Admin_QLGH")">Xem Thêm</a>
    </div>
</li>